/**
 * Database test helpers — scaffolded for Phase 1+.
 *
 * Add helpers here as domain models are implemented:
 *   clearDatabase()    — truncate all tables between tests
 *   seedTestData()     — insert known fixtures
 *   withTransaction()  — wrap test in a rolled-back transaction
 */

// Phase 1+: import db from "../../src/infrastructure/database/client.js"

export {};
